# Deps for the Brax + MJX GPU training pipeline (Lambda) and for
# rendering videos from MJX checkpoints (any machine).
#
# Installation:
#   pip install -r requirements.txt
#
# On Lambda with CUDA 12, also install the CUDA plugin:
#   pip install "jax[cuda12]==0.9.2"
# (Do NOT install jax[cuda12] on Mac — the plain jax below is correct
# for CPU rendering.)

# Physics
mujoco==3.6.0
mujoco-mjx==3.6.0

# JAX (CPU/Metal build — override with jax[cuda12] on Lambda)
jax==0.9.2
jaxlib==0.9.2

# RL training
brax==0.14.2
flax==0.12.6
optax==0.2.8
orbax-checkpoint==0.11.33
tensorboardX==2.6.5

# Utilities
numpy==2.4.4
matplotlib==3.10.8
pyyaml==6.0.3
rich==14.3.3

# Video rendering (render_mjx.py)
imageio==2.37.3
imageio-ffmpeg==0.6.0
pillow==12.2.0
